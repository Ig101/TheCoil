import { UserManagementFormGroupDirective } from './user-management-form-group.directive';

describe('UserManagementFormGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new UserManagementFormGroupDirective();
    expect(directive).toBeTruthy();
  });
});
